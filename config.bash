#!/bin/bash +x
# Config info for shell scripts.

# I really wish I could find a single instance way of tracking this data
# Perhaps some config text file file that can be parsed by both shell script and PHP
# while still being human readable.

export TZ='America/Chicago'
export PHP='/usr/bin/php'
export NOW=`date "+%Y-%m-%d %H:%M:%S"`

